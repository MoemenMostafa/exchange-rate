export class AppConfig {
    serverURL = 'https://api.exchangeratesapi.io';
    apiEndPoint = '';
    ms = {
        latest: {
          base:'/latest'
        },
        history:{
          base:'/history',
        }
    };
    errorMsg = {
    }
}
